# Optional: paste a captured session to skip the fetchproxy browser bridge.
# Both must be set together. Capture them from a signed-in remind.com tab.
REMIND_COOKIE=
REMIND_CSRF_TOKEN=
# fetchproxy bridge concentrator port (shared across the fleet).
REMIND_WS_PORT=37149
